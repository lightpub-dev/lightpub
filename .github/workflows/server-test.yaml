name: server-test

on:
  push:
    branches:
      - master

jobs:
  test:
    name: Run tests
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v3
      - name: Run tests
        run: |
          cd rs
          docker compose -f docker-compose.test.yml up
